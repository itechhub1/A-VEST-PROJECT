import { CustomResponse } from './customResponse';
import { ValidationError } from 'express-validator';
export declare class validationError extends CustomResponse {
    error: ValidationError[];
    statusCode: number;
    constructor(error: ValidationError[]);
    serializedError(): {
        message: any;
        feild: string;
    }[];
}
