export declare abstract class CustomResponse extends Error {
    abstract statusCode: number;
    constructor(message: string);
    abstract serializedError(): {
        message: string;
        field?: string;
    }[];
}
