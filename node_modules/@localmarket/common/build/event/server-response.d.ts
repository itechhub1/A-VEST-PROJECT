import { CustomResponse } from "../errors/customResponse";
export declare class ServerResponse extends CustomResponse {
    message: string;
    statusCode: number;
    constructor(message: string, statusCode: number);
    serializedError(): {
        message: string;
    }[];
}
